
namespace ProberApi.MyConstant {
    public static class ConfigKey {
        public const string CONTROL_MODE = nameof(CONTROL_MODE);

        public const string LISTENING_IP = nameof(LISTENING_IP);
        public const string LISTENING_PORT = nameof(LISTENING_PORT);

        public const string GUI_LANGUAGE = nameof(GUI_LANGUAGE);
    }
}
