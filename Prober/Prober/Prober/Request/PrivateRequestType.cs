namespace Prober.Request {
    internal static class PrivateRequestType {
        internal const string MOVE_TO_DUT = nameof(MOVE_TO_DUT);
        internal const string SAVE_IMAGE = nameof(SAVE_IMAGE);  
        internal const string SET_LIGHT_COLOR = nameof(SET_LIGHT_COLOR);
    }
}
